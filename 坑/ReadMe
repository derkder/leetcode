最后一定要加上‘\0’！！！！


函数的顺序问题，被调用的一定要在前面


一个个赋值要用字符串数组不要用字符串，然后s=arr这样赋值


用word_set.erase(word);就heap溢出，先用vector<>装，最后再一起erase就没事儿，很奇妙
可能说明在wordset中遍历的时候，不能删除wordset里的元素！！！！！！！


浮点数运算陷阱

dfs中，前面要visited[i][j]=1;后面更要visited[i][j]=0！！！！！！！！
