BFS:
看到最短首先想到的就是广度优先搜索//!!!!!!!
BFS在每一轮初始可以用!!!!!!!!queue.size!!!!!!一下子把上一轮放入队中的全出完//但好像都不这么做的
要记录bfs遍历层数的时候：!!!!!!!!queue.size!!!!!!

DFS:
现在我非常不赞同的一点：！DFS是不会回退visited数组状态的！，这个和回溯是不一样的
知乎：回溯算法 = 树的深度优先搜索 + 剪枝函数 也就是dfs也是需要回退状态的


!!!!!一般剪枝指的是跳出循环break continue，而不是跳出递归，return false这种,一用return false跳出递归答案就错


拓扑：
看到有向无环图DAG我们想到什么！
拓扑！！！！
