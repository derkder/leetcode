BFS:
看到最短首先想到的就是广度优先搜索//!!!!!!!
BFS在每一轮初始可以用!!!!!!!!queue.size!!!!!!一下子把上一轮放入队中的全出完//但好像都不这么做的
要记录bfs遍历层数的时候：!!!!!!!!queue.size!!!!!!





!!!!!一般剪枝指的是跳出循环break，而不是跳出递归，return false这种,一用return false跳出递归答案就错
