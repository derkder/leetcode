课程表1与2：
核心是拓扑排序问题
简单的法一：【经典拓扑排序的解法】
主要是使用这样的bfs就可以判断有向图是否有环了
就是入队入度为0的点，然后出队时放入结果并把对应的点删除也就是将该点指向的点的入度--；
复杂的法二：
dfs；其中每个点有未搜索、在搜索、搜索完成三个状态；
每次挑选状态1的点开始搜索，如果搜索到状态2即在搜索的点说明有环，将处在第三个状态的点入栈
最后搜索完毕栈里面的数据就是结果
