strcmp(s1, s2); //如果 s1 和 s2 是相同的，则返回 0；如果 s1<s2 则返回值小于 0；如果 s1>s2 则返回值大于 0。

s1.append(s2);   //不能用a1.append("def")，vector没有append

s1.insert(2,"def"); 

s1.erase(1,2); //从1位开始的两个字符

string s="abcdefg";
string s1=s.substr(1,3);   //bcd
string s2=s.substr(4);     //efg
substring第二个参数是长度不是截至的位置！！！


string str4 ( "abcdefghijk",3,5);   //从第三个位置开始取5个字符


string可以用“+”拼接欸

判断字符是否是数字的直接api：
isdigit








一定需要刻在脑子里的东西：
int a; string temp;
a = stoi(temp);
temp = to_string(a);
string temp = substr(int pos = 0, int n = npos);//返回由pos开始的n个字符组成的字符串
